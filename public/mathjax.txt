
$m = \frac{d}{z}$ 需满足$m \geq 2.5$

$$
\begin{aligned}
F &= ma \\
W &= F \cdot d
\end{aligned}
$$

基于您提供的实验单数据（JSON格式）和字段说明，我对数据进行了详细分析。数据中仅包含一个实验单（配方名称为“DD13”，类名为“HLYFPF”），这意味着我只能基于单个数据点提取静态规律，无法推断多变量间的动态趋势（如参数变化对结果的影响）。数学规律的归结需要多组实验数据（例如，不同工艺参数或物料配比的多个实验单）来建立模型（如回归分析、比例关系或相关性）。以下是我的分析步骤和结果：

### 分析步骤
1. **数据清洗与提取**：
   - 使用Python pandas库解析JSON数据，并基于提供的字段说明映射属性标签。
   - 提取关键数值字段：物料用量、占比、工步工艺参数、检测值（由于“REL_DETECTIONVALUE”字段数据混乱，部分值无法解析为数字，因此优先使用“REL_DETECTRATING”作为检测值的代理，因为它包含可用数值或范围）。
   - 过滤无效数据：例如，“REL_DETECTIONVALUE”中的“热441”和“fd地方r32”无法直接转换为数字，故忽略或视为异常；类似的字符串字段未用于数值分析。

2. **关键数据结构**：
   - **配方（HLYFPF）**: DD13，创建于2025-05-22。
     - **工艺（SYS_CRAFT）**: 包括一个工步（SYS_CRAFTWS），工艺参数为32（单位未指定，可能为温度、时间或比例）。
     - **检测项目（WEXP_DETECTITEM）**: 三个项目，名称“0401”、“AD”、“ddde”，部分有数值型“REL_DETECTRATING”。
     - **检测指标（WEXP_JCZB）**: 每个检测项目有多个质量标准的指标限度（例如，GB、QB、NB），用作参考范围。
   - **物料（LSWL）**: 三个临时物料，名称为“湿巾研发”、“护理研发1”、“测试2.1.1”。
     - 每个物料在“FormulaMaterialList”中都有用量（REL_DOSAGE）和占比（REL_PROPORTION）。
   - **样品（FORMUALSAMPLE）**: 一个样品，但未提供实测重量或成本等数值，故未分析。
   - 其他辅助字段（如创建时间、状态）未参与数学分析。

3. **数学规律归结方法**：
   - 基于可用数值计算比例、总和和平均值。
   - 由于仅一个数据点，**无法建立回归模型或相关性分析**（例如，工艺参数对检测值的影响）。规律局限于静态描述。
   - 重点分析：
     - 物料配比的比例关系。
     - 检测值与指标限度的符合程度（基于“REL_DETECTRATING”和“NDICATORLIMIT”）。
     - 潜在数字关系（如比例或差值）。

### 关键数学规律归纳
以下规律基于数据中的数值字段。所有规律均使用提取的实际数据（具体值见附件表），并辅以公式描述。

1. **物料配比规律：等比例混合**  
   - 三个物料（“湿巾研发”、“护理研发1”、“测试2.1.1”）的用量均为10（单位未指定，但“UNITDW”为“g”，可能为克），总用量为30。
   - 每个物料占比均为33.33%（“REL_PROPORTION”），计算如下：
     - 单个物料占比 = \( \frac{\text{用量}}{\text{总用量}} \times 100\% = \frac{10}{30} \times 100\% \approx 33.33\% \)。
   - **规律公式**：物料配比为简单等比例，即 \( \text{占比}_i = \frac{\text{用量}_i}{\sum \text{用量}} \times 100\% \)，其中 \( i = 1,2,3 \) 且用量相等。
   - **解释**：这表明实验设计采用均匀混合策略，每个物料贡献相同质量。总质量守恒（总用量为30）。

2. **工艺参数规律：单一工步固定值**  
   - 工步“DA1”的工艺参数（“CRAFTPARAMETER”）为32（单位未指定，可能为温度℃、时间min或比例值）。
   - **规律描述**：由于只有一个工艺数据点，没有变化范围，因此工艺参数为常数32。无法推断其对其他变量的影响。数学上，这可以表示为：工艺参数 \( P = 32 \)（恒定值）。

3. **检测值规律：数值离散，但部分符合指标范围**  
   - 检测项目有三个，但“REL_DETECTIONVALUE”字段数据无效（如“热441”和“fd地方r32”无法解析），因此使用“REL_DETECTRATING”作为代理值（该字段在部分数据中为数值，但字段说明中其标签为“指标限度”，这里用于分析实测值）。
     - “0401”：“REL_DETECTRATING”为“0-1”，可能代表范围。
     - “AD”：“REL_DETECTRATING”为数值45。
     - “ddde”：“REL_DETECTRATING”为数值342。
   - **数值规律**：
     - 两个可用数值检测值（45 和 342）差异大：比值为 \( \frac{342}{45} \approx 7.6 \)，差值为342 - 45 = 297。
     - 无直接线性关系：工艺参数32与45或342无明显比例（如45/32≈1.406，342/32=10.6875，无整数倍）。
     - 平均值：\( \frac{45 + 342}{2} = 193.5 \)，但统计意义弱，因数据点少。
   - **与指标限度的比较**（基于“CheckItemQuotaList”中的“NDICATORLIMIT”）：
     - “AD”的指标限度为“2-99”和“32-99”（不同质量标准），实测值45在此范围内，符合规范（45 ∈ [2,99] 和 [32,99]）。
     - “ddde”的指标限度为“21-78”和“432-777”，实测值342不在第一范围（342 ∉ [21,78]），但可能在第二范围（342 < 432, 故342 ∉ [432,777]），可能超标（需确认质量等级，但数据不足）。
     - “0401”的指标限度为“0-1”、“0-2”、“0-3”，由于实测值是范围“0-1”，它满足所有限度。
   - **规律公式**：检测值 \( V \) 与指标限度 \( [L_{\text{min}}, L_{\text{max}}] \) 的符合性可表示为布尔条件：  
     - 若 \( V \) 为数值，则检测通过当 \( L_{\text{min}} \leq V \leq L_{\text{max}} \)。  
     - 若 \( V \) 为范围（如“0-1”），则需检查其是否在指标限度的子集内（在此数据中满足）。

4. **整体数值关系：比例守恒但缺乏动态趋势**  
   - 物料总用量（30）与工艺参数（32）无直接数学关系（例如，30 ≠ k*32 for any simple k）。
   - 检测值与物料用量：检测值45和342与总用量30的比例为1.5:11.4（如45/30=1.5, 342/30=11.4），但无实用规律。
   - **关键洞察**：唯一明显数学规律是物料配比的等比例性（1:1:1）。其他数字（如工艺参数32、检测值45和342）孤立，无法建立模型。

### 数据汇总表
为清晰起见，所有关键数值总结如下表：

| **类别** | **项目** | **值** | **单位** | **规律描述** |
|----------|----------|--------|----------|--------------|
| **物料 (LSWL)** | 湿巾研发 (用量) | 10 | g（假设） | 用量均等，占总比33.33% |
|          | 护理研发1 (用量) | 10 | g（假设） | 用量均等，占总比33.33% |
|          | 测试2.1.1 (用量) | 10 | g（假设） | 用量均等，占总比33.33% |
|          | **总用量** | 30 | g（假设） | 守恒：30 = ∑(10) |
| **工艺 (SYS_CRAFTWS)** | DA1 (工艺参数) | 32 | 未指定 | 常数：P = 32 |
| **检测 (WEXP_DETECTITEM)** | 0401 (REL_DETECTRATING) | "0-1" | 无 | 范围，符合指标限度 |
|          | AD (REL_DETECTRATING) | 45 | 无 | 数值，在指标限度[2,99]内 |
|          | ddde (REL_DETECTRATING) | 342 | 无 | 数值，部分超标（不在[21,78]或[432,777]内） |
| **指标限度 (WEXP_JCZB)** | AD (NDICATORLIMIT) | "2-99", "32-99" | 无 | 实测45通过 |
|          | ddde (NDICATORLIMIT) | "21-78", "432-777" | 无 | 实测342未通过[21,78]，但342<432故未通过[432,777] |
|          | 0401 (NDICATORLIMIT) | "0-1", "0-2", "0-3" | 无 | 实测"0-1"通过 |

### 限制与建议
- **当前数据限制**：仅一个实验单数据，无法推断因果或动态规律（如工艺参数变化如何影响检测值）。数学规律仅限于描述性比例（物料配比）。
- **数据质量问题**：部分字段（如“REL_DETECTIONVALUE”）包含无效字符，需在后续实验中验证或清洗。建议使用纯数值字段记录检测结果。
- **后续实验建议**：
  - **增加数据点**：进行多个实验，系统性地改变变量（如物料用量或工艺参数），以拟合数学模型。
  - **潜在分析方向**：
    - **比例优化**：如果检测值是关键指标（如AD值45），可调整物料配比（例如，增加某物料用量）建立回归方程（如检测值 = a * 用量 + b * 工艺参数）。
    - **敏感性分析**：例如，固定物料配比，改变工艺参数32，观察检测值变化；或反之。
    - **质量控制模型**：基于指标限度和实测值，定义合格率函数（如二项分布）。
  - 提供完整实验数据（多个JSON）后，我可使用工具（如Python的SciPy或scikit-learn）进行统计分析、聚类或回归建模。

如果您有更多实验数据（多个实验单的JSON），请分享，我将进一步分析数学规律（如线性拟合、相关系数计算）。当前分析基于数据现状，保持客观描述。